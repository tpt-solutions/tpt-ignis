# TPT Ignis — Project Checklist

Derived from `spec.txt` and the approved implementation plan. Phase 0 is prerequisite work in the sibling `tpt-keystone-db` repo (not part of this repo's own workspace); Phases 1-4 mirror spec.txt §8's roadmap.

---

## Phase 0: `tpt-keystone-db` prerequisites (bulk COPY + array-capable UDFs)

*Lives in `c:/Programming/tpt-keystone-db`, not in this repo — tracked here because tpt-fluxstream's Phase 1 ingestion depends on it.*

- [ ] 0a. Add COPY protocol client support to `tpt-sdk`:
  - [ ] Add `CopyInResponse`/`CopyOutResponse`/`CopyData`/`CopyDone` to `tpt-sdk`'s own (client-side) `BackendMessage` enum in `keystone/wire.rs`, parsed in `read_message`
  - [ ] Add `write_copy_data`/`write_copy_done`/`write_copy_fail` frontend writers in `keystone/wire.rs`
  - [ ] Add `KeystoneClient::copy_in(table, columns, rows) -> Result<u64, KeystoneError>` in `keystone/mod.rs`, encoding rows in the same tab-delimited/backslash-escape text format `executor::copy::encode_copy_line` expects server-side
  - [ ] Client-driven `copy_in`/`copy_out` round-trip integration test against a live `tpt-keystone` instance
- [ ] 0b. Array/bytea-capable WASM UDF parameters:
  - [ ] Extend `tpt-sdk`'s `Value` enum with a `FloatArray(Vec<f64>)` variant + wire text-format encode/decode
  - [ ] Extend `executor/udf.rs`'s `wasm_type()` to accept an array/bytea `ColumnType` instead of `bail!`-ing
  - [ ] Implement pointer+length ABI in `executor/udf.rs::call()`: UDF module exports `memory` + `alloc(len: i32) -> i32`; host writes array bytes via `Memory::write`, calls UDF with `(ptr, len)`; UDF returns a packed `i64` (`(ptr << 32) | len`)
  - [ ] Add fuel-exhaustion (infinite-loop UDF) and trap (`unreachable`/OOB memory) test cases to `tpt-keystone-db`'s Linux CI job — *not runnable locally on Windows, `wasmtime::traphandlers::catch_traps` crashes the test process here (`STATUS_STACK_BUFFER_OVERRUN`); requires the Linux CI runner*
- [ ] Verify Phase 0 end-to-end against a running local `tpt-keystone` instance before starting tpt-fluxstream's ingestion wiring

## Phase 1: Foundation & Data Ingestion (Months 1-2)

- [ ] Initialize tpt-ignis Cargo workspace (root `Cargo.toml`, `rust-toolchain.toml` pinned to 1.90.0, `deny.toml`, `LICENSE` (Apache-2.0), `.gitignore`, `README.md`, `CLAUDE.md`)
- [ ] Configure CI (`.github/workflows/ci.yml`): three-repo checkout (tpt-ignis/tpt-keystone-db/tpt-appfront as siblings), fmt, clippy, `cargo build --workspace`, `cargo nextest run --workspace`, `cargo deny check`
- [ ] Scaffold all workspace members as compiling stubs: `tpt-plasma-schemas`, `tpt-physics-math`, `tpt-ui-components`, `tpt-fluxstream`, `tpt-halo`, `tpt-aether`, `tpt-oracle`
- [ ] Implement `tpt-plasma-schemas`: IMAS-inspired structs (`magnetics`, `interferometer`, `thomson_scattering`, `pulse_schedule`), `units.rs` newtypes, msgpack `to_msgpack`/`from_msgpack` helpers
- [ ] Implement `tpt-fluxstream` synthetic data generator (`synth.rs`): IMAS-shaped signals (MHD-mode-like sine + Gaussian noise, disruption-like amplitude ramp)
- [ ] Implement `tpt-fluxstream` native FFT (`signal.rs`, `rustfft`) as instability-detection test oracle
- [ ] Implement `tpt-fluxstream` ingestion (`ingest.rs`): schema DDL, bulk `copy_in` batched loads via `tpt-sdk` (depends on Phase 0a), in-DB `detect_instability` UDF invocation (depends on Phase 0b) cross-checked against the native oracle
- [ ] Implement `udf-fft` crate: FFT UDF compiled to `wasm32-unknown-unknown`/`wasm32-wasip1` using the Phase 0b pointer+length ABI, registered via `CREATE FUNCTION ... LANGUAGE wasm`
- [ ] `mastu.rs` real MAST-U/ITER open-data loader — *optional stretch goal, feature-gated (`mastu-loader`), network/format-tooling gated, not required for Phase 1 completion*
- [ ] **Milestone: synthetic diagnostic data streams end-to-end from tpt-fluxstream into tpt-keystone-db via bulk COPY, with in-DB FFT UDF results matching the native oracle**

## Phase 2: Physics Math & Visualization (Months 3-4)

- [ ] `tpt-physics-math`: basic magnetic flux surface calculations (beyond the Phase-1 stub `cylindrical_to_toroidal` fn)
- [ ] `tpt-halo`: real native + WASM targets via `appfront-canvas`/`appfront-tui` (beyond the Phase-1 stub static `UITree` window)
- [ ] Basic 3D-ish rendering of a tokamak torus using appfront's actual egui+glow renderer (no wgpu — appfront doesn't have it) and connect to the live data stream from tpt-fluxstream
- [ ] `tpt-ui-components`: real Plasma Gauge / diagnostic-shot charting widgets (beyond the Phase-1 stub single gauge fns)

## Phase 3: Digital Twin & Topology (Months 5-6)

- [ ] `tpt-aether`: real implementation using Graph/Geospatial engines of tpt-keystone-db (beyond the Phase-1 stub single-query smoke test) — mock reactor's physical components and cooling loops
- [ ] Integrate `tpt-halo` to overlay live thermal data onto the `tpt-aether` geospatial model

## Phase 4: AI & Agent Integration (Months 7-8)

- [ ] `tpt-oracle`: real Vector engine (Prism) integration for historical state matching (beyond the Phase-1 stub single-query smoke test)
- [ ] Expose the system via an MCP server (`tpt-keystone`'s existing `mcp` module / `appfront-mcp`) and JSON-LD schemas (`appfront-ai-schema`), allowing a basic LLM agent to query plasma state and suggest parameter adjustments

---

## Known limitations carried forward from spec.txt (tracked, not blocking)

- `tpt-keystone-db`'s six "engines" (Meridian/geo, Prism/vector, Chronos/timeseries, Plexus/graph, Canopy/document, relational core) are single-node/local-only — no distributed secondary indexes yet. Fine for Phase 1-4 of this platform; a real production reactor deployment would need that solved upstream first.
- `tpt-sdk`'s wire client is text-format only (no binary-format params) — acceptable at Phase 1 data volumes; revisit if/when ingestion throughput becomes the bottleneck.
- No sibling suite currently path/git-depends on `tpt-keystone-db`/`tpt-appfront` — this repo establishes that convention for the first time; there's no prior art elsewhere in the codebase to fall back on if the path-dependency approach hits friction.
